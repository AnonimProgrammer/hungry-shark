# Game restart

**What I asked the AI:** Make the game restartable without refreshing the page for the mandatory `feat: game restart` milestone.

**What it gave me:** A `resetGame()` function that restores shark HP/position, score, hunger, fish school, and bomb; a **Play Again** button on the game-over screen that calls the same `startGame()` flow as the start screen.

**What was wrong:** Nothing

**How I fixed it:** Nothing

**Time lost:** ~0 minutes
