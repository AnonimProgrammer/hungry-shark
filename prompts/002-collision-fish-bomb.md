# Collision — fish & bomb

**What I asked the AI:** Add a new entities (fish + bomb) and implement simple collision/interaction logic for the mandatory `feat: collission implemented` milestone.

**What it gave me:** `Fish` and `Bomb` classes with `.draw()` and fish wandering; global `checkCollision()` using circle overlap; shark eats common fish (+5 HP, respawn) and takes bomb damage (−30 HP, red flash).

**What was wrong:** Nothing

**How I fixed it:** Nothing

**Time lost:** ~0 minutes
