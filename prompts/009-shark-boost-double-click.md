# Shark boost on double-click

**What I asked the AI:** Add shark boost on double-click (2× speed).

**What it gave me:** Boost state machine on `Shark` (`READY` / `ACTIVE` / `COOLDOWN`), double-click detection via `mousedown` `event.detail`, 2× speed while boost is active, HUD boost meter with progress bar, constants wired from `constant.js`.

**What was wrong:** Nothing reported — first implementation.

**How I fixed it:** N/A

**Time lost:** ~0 minutes
