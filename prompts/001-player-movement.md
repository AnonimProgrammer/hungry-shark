# Player movement — Shark class & mouse swim

**What I asked the AI:** Implement the first Movement/Interaction milestone: get one entity (the shark) moving on screen with keyboard or mouse, following the project design (mouse aim + LMB hold to swim). Update AI_DIARY and create a prompt log entry.

**What it gave me:** A `Shark` class with `rotateToward`, `moveForward`, and `draw` methods; canvas input listeners for mouse position and LMB; a `requestAnimationFrame` loop that clears the canvas, draws sky/water/seabed zones, and updates the shark each frame.

**What was wrong:** Nothing

**How I fixed it:** Nothing

**Time lost:** ~0 minutes
